kind: 'Fixed'
body: 'fixed the **Source** filter on the **Identities** tab drawing its label across the selected option — a native `select` always renders whichever option is current, so its label has to be shrunk unconditionally'
time: '2026-09-15T15:35:15.842464346Z'
